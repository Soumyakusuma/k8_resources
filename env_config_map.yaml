apiVersion: v1
kind: ConfigMap
metadata:
  name: envconfig
  labels:
    name: envconfiglearn
spec:
  containers:
    - name: nginx
      image: nginx
      envFrom:
        - cofigMapRef:
            name: configmap
